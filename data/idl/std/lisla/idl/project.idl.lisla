(package lisla.idl)

(import lisla.util.file)
(import lisla.idl.library)

(struct ProjectConfig
    (lisla_home? DirectoryPath)
    (description? String)
    (idl.. DirectoryPath)
    (extension..< FileExtensionDeclaration)
)

;; 'Types for Lisla Project'